# Google Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent

# Together AI Configuration
TOGETHER_API_KEY=your_together_api_key_here

# Development Mode
NODE_ENV=development

# Note: This is a sample .env.local file. Create a .env.local file with your actual API keys.
# For production, get your own API keys at:
# - Google Gemini: https://ai.google.dev/
# - Together AI: https://www.together.ai/

# Supabase Configuration
# You need to replace these values with your actual Supabase project details
# IMPORTANT: Make sure to update both the anon key and service role key to match the new project URL
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
# Update this with the anon key from your new project
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
# Service Role Key (for admin operations that bypass RLS)
# You need to get this from your Supabase project settings
# IMPORTANT: This key has admin privileges and should never be exposed to the client
# It should only be used in server-side code
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here